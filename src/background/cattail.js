import colours from '../colours';

class Cattail {
  constructor(canvas, ctx, pos) {
    this.canvas = canvas;
    this.ctx = ctx;
    this.pos = pos;
  }

  render() {
    
  }

}

export default Cattail;
